---
description: Drop this repo's lazy-developer rules for the rest of the session
---

Ignore the rules in `CLAUDE.md` for the remainder of this session.

Specifically: the ladder, the "no abstractions that weren't explicitly
requested" rule, the deletion-over-addition and fewest-files preferences, and
the `ponytail:` comment convention no longer apply. Build it the way you would
in a repo with no house style.

Still not optional, because these were never about laziness: understanding the
problem before changing it, input validation at trust boundaries, error
handling, security, and accessibility.

Say "ponytail rules off" once, then carry on with whatever the user asked for.
